```
Usage: spotify-backup <COMMAND>

Commands:
  playlist  Prints playlist to stdout as JSON
  liked     Prints liked songs to stdout as JSON
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help
```